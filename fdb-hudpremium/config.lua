-- ============================================================
-- fdb-hudpremium | config.lua
-- Configurações gerais do HUD e Núcleos
-- ============================================================

Config = {}
lib.locale()

Config.Vitals = {
    MaxArmor = 100,      -- Valor máximo do Colete/Armadura nativo (0 a 100)
    MaxOxygen = 10.0     -- Tempo máximo de fôlego subaquático em segundos (padrão RedM)
}
